% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brm-hash-core.R
\name{hash_data}
\alias{hash_data}
\title{Hash or fingerprint a data.frame for caching}
\usage{
hash_data(
  data,
  threshold = 1e+07,
  mode = c("auto", "signature", "full"),
  algo = "xxhash64",
  include_summary = FALSE
)
}
\arguments{
\item{data}{A data.frame to fingerprint.}

\item{threshold}{Cell count above which the *auto* mode falls back to dim-only hashing.}

\item{mode}{One of `"auto"`, `"signature"`, or `"full"`.}

\item{algo}{Hash algorithm passed to \link[digest]{digest}.}

\item{include_summary}{Logical; if `TRUE` the signature includes basic
column summaries (used only when `mode = "signature"`).}
}
\value{
A character hash.
}
\description{
* **mode = "auto"**   (default)
  ‒ Hashes the full data if `nrow * ncol <= threshold`;
  ‒ otherwise hashes only its dimensions to stay fast.
}
\details{
* **mode = "signature"**
  ‒ Creates a compact, human-readable signature (row/col count, names, types, small
     value summaries) and hashes that signature.

* **mode = "full"**
  ‒ Always hashes the complete data.frame (may be slow for very large data).
}
